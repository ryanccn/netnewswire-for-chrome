{
  "name": "netnewswire-for-chrome",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "rm -rf dist/ && mkdir dist/ && cp -r public/* dist/ && esbuild background.ts content.ts --outdir=dist/ --format=esm --minify --bundle"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.206",
    "typescript": "^4.9.4",
    "esbuild": "^0.16.15"
  }
}
